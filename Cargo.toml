[package]
name = "dapper"
version = "0.1.0"
edition = "2021"

[dependencies]
tree-sitter = { version = "*", features = ["wasm"] }
tree-sitter-dapper = { path = "tree-sitter" }
indexmap = "*"

[build-dependencies]
cc = "*"
